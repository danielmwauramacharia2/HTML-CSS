
---

## 🧾 summary.md

```markdown
# Lesson 2: CSS Basics

**Goal:** Learn to style web pages using CSS.

**Core Ideas:**
- CSS changes how HTML looks — colors, shapes, sizes, spacing.
- Written inside `<style>` tags or `.css` files.
- Syntax: `selector { property: value; }`
- Use classes (e.g. `.subscribe-button`) to style specific elements.
- Units: pixels (`px`)
- Common properties:
  - `background-color`
  - `color`
  - `border`, `border-radius`
  - `margin`, `height`, `width`
  - `font-weight`, `font-size`
  - `cursor`
- Trial and error is part of design.
